name: TestAction1

on:
  workflow_dispatch:

jobs:
  test-action1:
    runs-on: ubuntu-latest

    steps:
    - id: def
      run: |
        echo "a=2024-09-16" >> $GITHUB_OUTPUT
        echo "b=2024-09-17" >> $GITHUB_OUTPUT
    
    - run: echo 1
      if: steps.def.outputs.a < steps.def.outputs.b
    
    - run: echo 2
      if: steps.def.outputs.a > steps.def.outputs.b
    
    - run: echo 3
      if: steps.def.outputs.a == steps.def.outputs.b
